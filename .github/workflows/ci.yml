name: CI

on:
  push:
    branches: [dev, main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: 测试
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Localizable.strings 一致性
        run: python3 lyli/scripts/check_strings_parity.py

      - name: Swift selftest
        run: |
          cd lyli
          swift build --product lyli-selftest
          ./.build/debug/lyli-selftest

      - name: 编译整个 App
        run: cd lyli && swift build
